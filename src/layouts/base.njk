<!DOCTYPE html>
<html lang="{{ meta.lang }}">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ meta.title }} - {{ meta_title }}</title>
    <link rel="preload" href="{{ '/assets/fonts/merriweathersans-bold-webfont.woff2' | url }}" as="font" type="font/woff2" crossorigin>

    <link rel="stylesheet" href="{{ '/assets/styles/main.css' | url }}">
    {% include "meta.njk" %}
</head>
<body>
    <div hidden>{% iconsprite %}</div>

    <a href="#content" class="sr-skip-link">Skip to main content</a>

    <div class="layout container" role="document">
        {% include "header.njk" %}
        <main id="content" class="main">
            {{ content | safe }}
        </main>
    </div>
    <script type="text/javascript" src="{{ '/assets/scripts/main.js' | url }}"></script>
</body>
</html>